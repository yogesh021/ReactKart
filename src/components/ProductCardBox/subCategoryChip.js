import React from "react";
import Chip from "@material-ui/core/Chip";
import Avatar from "@material-ui/core/Avatar";

const SubCategoryChip = (props) => {
  return (
    <Chip
      size="medium"
      color="primary"
      label="Television"
      // onClick={handleClick}
    />
  );
};

export default SubCategoryChip;
